{% extends 'CancellarAdminBundle::layout.html.twig' %}

{% form_theme form 'CancellarAdminBundle::form.html.twig' %}

{% block main %}
<h2>Fordulo konfiguralasa</h2>
<form class="wide" method="post" {{ form_enctype(form) }}>
  <fieldset>
    <legend>Adatok</legend>
    {{ form_row(form.name) }}
    {{ form_row(form.filetypes) }}
    {{ form_row(form.size) }}
    {{ form_row(form.categories) }}
    {{ form_rest(form) }}
    <div class="formrow row_required">
      <p class="inputholder">
      <input type="submit" value="Mentés" class="button" />
      </p>
    </div>
  </fieldset>
</form>
{% endblock %}

